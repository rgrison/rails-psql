production: # Environment
    first_thing: # Hook point
      - command: sudo /bin/sh -c "echo $(grep -A 1 CLOUD66-AUTOMATED-CONTENT-DO-NOT-MODIFY /etc/hosts | sed 1d | awk '{print $2}') > /etc/hostname && service hostname restart"
        target: any # Hook field
        execute: true
        sudo: true # Hook point
